<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Notes.Views.Modal.EditNoteView">
  
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition Height="*"></RowDefinition>
      <RowDefinition Height="Auto"></RowDefinition>
      <RowDefinition Height="Auto"></RowDefinition>
    </Grid.RowDefinitions>
    <StackLayout Grid.Row="0" Padding="20">
      <Entry Text="{Binding Title}" Placeholder="Title"></Entry>
    </StackLayout>
    <StackLayout Grid.Row="1" Padding="20" VerticalOptions="FillAndExpand">
      <Editor Text="{Binding Content}" VerticalOptions="FillAndExpand"></Editor>
    </StackLayout>
    
    <Label Grid.Row="2" Text="{Binding ErrorMessage}"></Label>
    
    <StackLayout Grid.Row="3" Padding="20" Orientation="Horizontal">
      <Button Text="Save" Command="{Binding Save}"></Button>
      <Button Text="Delete" Command="{Binding Delete}" IsEnabled="{Binding IsEdit}"></Button>
    </StackLayout>
  </Grid>
  
</ContentPage>